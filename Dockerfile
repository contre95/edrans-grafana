FROM grafana/grafana:5.1.0
# RUN chown -R root /etc/grafana && \
# RUN chmod -R a+r /etc/grafana && \
# RUN chown -R grafana:grafana /var/lib/grafana && \
# RUN chown -R grafana:grafana /usr/share/grafana
